.gradle
/build/
!gradle/wrapper/gradle-wrapper.jar
*.class
bin/main/application.yaml

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans

### IntelliJ IDEA ###
.idea
/out
*.iws
*.iml
*.ipr

.DS_Store

applicationinsights-agent-*.jar
*.log

### Test brief, not part of the solution ###
Technical Test.docx

### Local environment ###
# Holds local database credentials. Never commit it.
.env
.env.local

### Terraform ###
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
# Per-environment values, no secrets. The pipeline selects one with -var-file.
!infrastructure/terraform/envs/*.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# .terraform.lock.hcl is deliberately NOT ignored: the provider lock file is
# committed so CI and every engineer resolve identical provider versions.
